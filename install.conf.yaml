- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Developer
    - ~/Work

- shell:
    -
      command: ./setup_homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
